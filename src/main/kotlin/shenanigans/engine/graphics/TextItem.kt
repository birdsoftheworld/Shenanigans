package shenanigans.engine.graphics


class TextItem constructor(private val text : String, fontFileName: String, final val numCols : Int, final val numRows : Int) /*: GameItem*/{

    private val VERTICES_PER_QUAD = 4
    //private val numCols : Int = col
    //private val numRows : Int = row
    //private val text : String = text

    //textures not set up yet
    //val texture = Texture(fontFileName)
    //this.setMesh(buidMesh(texture, numCols, numRows))



}