package shenanigans.engine.window

import shenanigans.engine.ecs.Resource
import shenanigans.engine.window.Window

class MouseInput(private val window: Window) : Resource {
    // needs an event queue
}